# BeeTrack — AI Assistant Backlog

> Living document. Treat each item like a Jira ticket — update status as work progresses.
> Design doc: [AI_ASSISTANT.md](AI_ASSISTANT.md) — read that first; these tickets implement it
> section by section. Replaces the old Epic 5 (Voice Logging) / Epic 7 (MCP Server)
> placeholder rows removed from [BACKLOG.md](BACKLOG.md).

---

## Status Legend

| Symbol | Meaning     |
| ------ | ----------- |
| `[ ]`  | To do       |
| `[~]`  | In progress |
| `[x]`  | Done        |
| `[!]`  | Blocked     |

---

## Epics Overview

1. [Voice-Logged Inspections](#1-voice-logged-inspections)
2. [AI Apiary Assistant (Chat + MCP)](#2-ai-apiary-assistant-chat--mcp)
3. [Shared Infrastructure](#3-shared-infrastructure)
4. [Migrate to OpenRouter](#4-migrate-to-openrouter)

---

## 1. Voice-Logged Inspections

> Ref: [AI_ASSISTANT.md](AI_ASSISTANT.md) §2. Recording is queued and processed by a
> background worker (§2.1), not handled inline in the HTTP request. The worker only
> **proposes** actions (§2.2) — nothing is written to `inspections`/`treatments`/
> `harvests`/`feedings`/`hives` until the beekeeper explicitly Accepts a recording's
> proposals (§2.6); Reject discards them, nothing to undo either way.

| ID       | Layer | Status | Title                                            | Notes                                                                                                                                    |
| -------- | ----- | ------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| VC-01-BE | `BE`  | `[x]`  | Migration: `voice_recordings` + `voice_actions`  | §2.4 schema — recording status enum incl. `completed`/`accepted`/`rejected`/`cancelled`; `voice_actions.status` (`proposed`/`applied`/`error`), `audio_path`, `previous_hive_state` (captured at Accept time, diagnostic only — no undo path); also `retry_count`/`next_attempt_at` for transient-Whisper-failure retry (§2.2, §2.4). Model + repository (Create/Get/List/Update/Delete on both tables) landed together, same as AST-17-BE — service/handler layers are separate later tickets |
| VC-02-BE | `BE`  | `[x]`  | `AUDIO_STORAGE_PATH` config + Docker volume       | Same UUID-filename convention as `IMAGE_STORAGE_PATH`; transient, worker deletes after transcription succeeds OR after retries are exhausted and the recording is marked `failed` (§2.4). Lives on the base `Config` (like `ImageStoragePath`), loaded unconditionally at startup — not on `AIConfig`, whose `validate()` hard-requires the Anthropic/OpenAI keys and would otherwise block server startup on those even for routes that don't need them yet; removed the redundant unused `AudioStoragePath` field `AIConfig` already had                                |
| VC-03-BE | `BE`  | `[x]`  | `internal/llm/` — Anthropic API client wrapper    | `internal/llm/client.go`: `Client` embeds the Anthropic SDK client, `NewClient(apiKey, opts...)` wires the API key in. Not yet called from `main.go` or the voice worker — that lands with AST-09-BE (chat) and VC-06-BE (voice worker) (§4) |
| VC-04-BE | `BE`  | `[x]`  | Whisper client wrapper                            | Hosted STT call; returns transcript + per-segment confidence for the quality gate (§2.2). `internal/llm/whisper.go`: `NewWhisperClient(apiKey, opts...)` + `Transcribe(ctx, audio, filename)`, hand-rolled multipart HTTP call (no official OpenAI Go SDK in go.mod) to `/audio/transcriptions` with `model=whisper-1`, `response_format=verbose_json` for per-segment `avg_logprob`/`no_speech_prob`. Not yet called from the voice worker (VC-06/07-BE)                                                   |
| VC-05-BE | `BE`  | `[x]`  | `POST /apiaries/{id}/voice` — enqueue endpoint    | Multipart upload; validates `RECORDING_TOO_LONG` server-side; saves audio, inserts `pending` row, returns `202 Accepted` (§2.1, §2.3). Two deliberate additions beyond the ticket: 15 MB file-size cap used as a server-side proxy for the client's ~3-minute recording duration limit (no way to check duration without decoding), and a 20-stored-recordings-per-user cap (`MAX_RECORDINGS_REACHED`, 422) to bound storage growth ahead of the worker (VC-06/07-BE) being built |
| VC-06-BE | `BE`  | `[x]`  | Voice worker: poll loop + status lifecycle        | `pending → processing → completed/failed`, same shape as the blockchain worker; stuck-`processing` recovery (worker crash/restart mid-job, distinct from Whisper-call retry below) (§2.1, §2.4). `internal/worker/voice_worker.go`'s `VoiceWorker` claims pending rows via `FOR UPDATE SKIP LOCKED` (mirrors `BlockchainWorker`'s `ClaimNext`); new `claimed_at` column (`044_voice_recordings_add_claimed_at.sql`) plus repository `SweepStuckProcessing` recovers rows stuck in `processing` past a 5-minute timeout. Wired up in `main.go`'s `startVoiceWorker`, gated on new `config.OpenAIAPIKey()` — optional/non-fatal if unset, same pattern as the blockchain worker and assistant wiring |
| VC-07-BE | `BE`  | `[x]`  | Worker: transcription + audio-quality gate + retry | `POOR_AUDIO_QUALITY` short-circuit before Claude is ever called (§2.2); transient Whisper failures (timeout/5xx/rate-limit) bump `retry_count`, set `next_attempt_at` (backoff 5s/30s/2m), and return to `pending` — up to `MAX_WHISPER_RETRIES` (3) before moving to `failed`; non-transient failures (corrupt file, 4xx) go straight to `failed`, no retry (§2.2, §2.4). `internal/llm/whisper.go` now returns a typed `WhisperAPIError` exposing the HTTP status code so the worker can classify transient vs non-transient. Phase 1/2 (hive resolution, action proposal) not implemented yet (VC-08/09-BE) — a recording passing the quality gate goes straight to `completed` with just its transcript stored, no `voice_actions` rows created |
| VC-08-BE | `BE`  | `[x]`  | Worker Phase 1: hive-name resolution              | Calls `list_hives` (shared with AST-02-BE, invoked directly as a Go function via `*mcp.HiveTools`, not over MCP transport) filtered to the current apiary, then a single non-streaming Claude call forced to call a `resolve_hive` tool (`outcome: matched\|not_identified\|multiple`) resolves the transcript to a single hive_id. `HIVE_NOT_IDENTIFIED` on no/ambiguous match (or a hallucinated `hive_id` not in the apiary's hive list — defensive fallback), or `MULTIPLE_HIVES_MENTIONED` if more than one distinct hive is named → either way written straight to a single `voice_actions` `error` row, `hive_id`/`tool_name` NULL, Phase 2 skipped (§2.1, §2.2). A zero-hive apiary skips the Claude call entirely and is treated as `HIVE_NOT_IDENTIFIED` (cost optimization beyond the ticket's wording); a `matched` outcome doesn't persist anything new yet since Phase 2/VC-09-BE isn't wired in — the recording just proceeds to `completed`. `main.go`'s `startVoiceWorker` now requires both `OPENAI_API_KEY` and `ANTHROPIC_API_KEY` to start the worker |
| VC-09-BE | `BE`  | `[x]`  | Worker Phase 2: propose actions                   | Only runs once Phase 1 (VC-08-BE) resolved exactly one hive_id. Fetches that hive's context (type, status flags, diseases, most recent inspection's frame counts) via `*mcp.HiveTools.GetHiveSummary` (now also on the `HiveLister` interface) with a 90-day lookback, then makes a second unforced Claude call offering `create_inspection` / `create_treatment` / `create_harvest` / `create_feeding` tool schemas (matching the existing `model.VoiceActionToolCreateInspection/Treatment/Harvest/Feeding` constants). Each recognized tool_use block becomes one `voice_actions` row (hive_id fixed, `tool_arguments` = raw unvalidated/unparsed JSON, `status = 'proposed'`, 1-indexed `sequence`); an unrecognized tool name is silently skipped. Zero recognized calls means zero `voice_actions` rows (the `NO_ACTION_RECOGNIZED` case). Hive-context fetch or Claude-call failure marks the recording `failed`, no retry, same as every other LLM-call failure in this pipeline. `update_hive_status` (VC-11-BE) and timestamp fields (`inspected_at`/`treated_at`/`harvested_at`/`fed_at`) deliberately not implemented here — left for VC-11-BE and the Accept-time endpoint (VC-15-BE) respectively — the service layer is deliberately NOT called here either; no validation happens at this stage (§2.1, §2.2) |
| VC-10-BE | `BE`  | `[ ]`  | Reuse autocomplete history as Claude context      | `GET /medicines`, `/feed-types`, `/feed-amounts` fed into Phase 2 so spoken values match existing entries instead of forking duplicates (§2.2) |
| VC-11-BE | `BE`  | `[ ]`  | `update_hive_status` tool                         | Wraps `PATCH .../hives/{hiveId}` (flags + diseases); proposed like every other action, only executed at Accept time, which is also when `previous_hive_state` is captured (§2.2, §2.6) |
| VC-12-BE | `BE`  | `[ ]`  | Multi-action splitting in one recording           | For the single resolved hive (VC-08-BE), Claude may emit 1..N write-tool calls per transcript (e.g. an inspection plus a feeding); each proposed independently, and later accepted/rejected together as one batch. Does not apply across hives — a recording naming more than one hive is rejected outright (`MULTIPLE_HIVES_MENTIONED`, VC-08-BE), not split (§2.1, §2.2, §2.6) |
| VC-13-BE | `BE`  | `[ ]`  | `GET /apiaries/{id}/voice-recordings`             | Paginated, newest first; nested `voice_actions` (with their `status`) once `completed`/`accepted`/`rejected` (§2.6)                          |
| VC-14-BE | `BE`  | `[ ]`  | `DELETE /apiaries/{id}/voice-recordings/{id}`     | Cancel — only while `status = 'pending'`; marks `cancelled`, deletes server-side audio file (§2.5)                                          |
| VC-15-BE | `BE`  | `[ ]`  | `POST /apiaries/{id}/voice-recordings/{id}/accept` | Only valid while `status = 'completed'`; runs every non-error proposed action's real service call independently, moves recording to `accepted` (§2.2, §2.6) |
| VC-16-BE | `BE`  | `[ ]`  | `POST /apiaries/{id}/voice-recordings/{id}/reject` | Only valid while `status = 'completed'`; hard-deletes the recording's `voice_actions` rows, moves recording to `rejected` — transcript/recording row kept (§2.2, §2.6) |
| VC-17-FE | `FE`  | `[ ]`  | Mic icon + recording dialog on `ApiaryGridScreen` | Bottom amber banner icon opens a modal (same pattern as `_HiveListDialog`); round record button inside (§2.1, §2.5)                          |
| VC-18-FE | `FE`  | `[ ]`  | Recording controls: toggle, hard cap, auto-stop   | Tap to start/stop; ~2.5s silence auto-stop with start grace period; hard cap (e.g. 3 min) with warning (§2.2)                               |
| VC-19-FE | `FE`  | `[ ]`  | Local audio storage + Play in pending list        | Save recording via `path_provider` before upload; Play button plays the local file (§2.5)                                                   |
| VC-20-FE | `FE`  | `[ ]`  | Cancel a pending recording (FE)                   | Calls VC-14-BE while `pending`; hidden once `processing` (§2.5)                                                                             |
| VC-21-FE | `FE`  | `[ ]`  | Voice Activity screen                              | History-icon button next to the mic icon; paginated recording list with status chips; tapping a recording opens transcript + proposed actions + Accept/Reject buttons (calls VC-15/16-BE) while `completed`; accepted actions tap through to the normal edit screen (§2.6) |
| VC-22-FE | `FE`  | `[ ]`  | l10n strings for voice logging UI                 | Recording dialog, statuses (incl. Awaiting review/Accepted/Rejected), Accept/Reject buttons, error copy (`NO_ACTION_RECOGNIZED`, `HIVE_NOT_IDENTIFIED`, `MULTIPLE_HIVES_MENTIONED`, `POOR_AUDIO_QUALITY`, `RECORDING_TOO_LONG`) |
| VC-23-BE | `BE`  | `[ ]`  | Migration: `voice_recordings.hive_id`             | Nullable, `REFERENCES hives(id)`; set only by the hive-scoped enqueue endpoint below (VC-24-BE), NULL for apiary-grid recordings even after Phase 1 resolves one — that per-action result stays on `voice_actions.hive_id` instead (§2.1, §2.4) |
| VC-24-BE | `BE`  | `[ ]`  | `POST /apiaries/{id}/hives/{hiveId}/voice`        | Hive-scoped sibling of VC-05-BE's endpoint; same multipart/validation/response shape, but sets `hive_id` on the inserted row from the URL (§2.1, §2.3) |
| VC-25-BE | `BE`  | `[ ]`  | Worker: skip Phase 1 when `hive_id` already known | If the claimed recording already has `hive_id` set (VC-23/24-BE), the worker skips the `list_hives` fetch and the Claude hive-resolution call entirely and goes straight to Phase 2 with that hive_id fixed — no cross-check against the transcript, the screen the recording was started from always wins (§2.1, §2.2) |
| VC-26-FE | `FE`  | `[ ]`  | Mic icon + recording dialog on `HiveDetailScreen` | Same recording dialog/controls as VC-17/18/19-FE, reused on the hive screen; uploads to VC-24-BE instead of VC-05-BE; no "say the hive name" hint needed here since the hive is already known (§2.1, §2.2) |
| VC-27-BE | `BE`  | `[x]`  | Migration + persist raw Claude request/response per call | `045_create_voice_llm_calls.sql` creates `voice_llm_calls` (`voice_recording_id` FK cascade-delete, `phase` CHECK IN `resolve_hive`/`propose_actions`, `request` JSONB NOT NULL, `response` JSONB, `is_error`, `created_at`) — one row per Phase 1/2 Claude call, success or failure, mirroring `assistant_tool_calls`'s audit-trail role for the chat assistant. `internal/worker/voice_worker.go`'s new `logLLMCall` helper is called at both existing Claude call sites (Phase 1 hive resolution, Phase 2 action proposal), persisting what was sent (transcript + hive list, or transcript + hive context) and Claude's raw response — or the raw error if the call failed. Best-effort write: a failure to persist this row is only logged, never fails the recording itself (§2.4) |

---

## 2. AI Apiary Assistant (Chat + MCP)

> Ref: [AI_ASSISTANT.md](AI_ASSISTANT.md) §3. Read-only in v1 — no write tools here;
> voice logging (Epic 1 above) is the write path.

| ID        | Layer | Status | Title                                    | Notes                                                                                                          |
| --------- | ----- | ------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| AST-01-BE | `BE`  | `[x]`  | `internal/mcp/` package + tool registry  | In-process, no separate deployment; tools call straight into existing repositories (§3.2)                       |
| AST-02-BE | `BE`  | `[x]`  | Tool: `list_hives`                       | Optional `apiary_id` filter, status flags, and active diseases — also called directly by VC-08-BE for hive-name resolution (§3.3). Excludes inactive hives (added alongside AST-05-BE, applies to every multi-hive tool via the shared `resolveHives` helper) |
| AST-03-BE | `BE`  | `[x]`  | Tools: `list_hive_records` + `get_hive_summary` | Deviates from the original single-tool sketch: `Treatment` has no active/inactive concept in the schema, so `list_hive_records(hive_id, record_types?, days?)` replaces a fixed "latest"/"active" shape with a recency window, and a `record_types` array lets Claude fetch several categories (e.g. inspections + feedings) in one call instead of one per type. `get_hive_summary` aggregates all four plus status flags/diseases over the same optional day window. (Originally shipped as 4 separate per-type tools, then collapsed into one — see AST-04-BE's note for why.) |
| AST-04-BE | `BE`  | `[x]`  | Tool: `list_hives_missing_records`       | Originally shipped as 3 separate tools (`list_untreated_hives`/`list_uninspected_hives`/`list_unfed_hives`), then collapsed into one `list_hives_missing_records(apiary_id?, record_types?, days?)` for the same reason as AST-03-BE: near-identical tools differing only by record type waste a tool call per category and clutter the tool list Claude has to choose from. Each result lists which requested types it's actually missing (§3.3) |
| AST-05-BE | `BE`  | `[x]`  | Tool: `list_hives_by_status`             | Widened from just `needs_food` to `(apiary_id?, statuses?)` covering queenless/needs_food/sick/ready_for_harvest — same collapse rationale as AST-03/04-BE. `sick` means the hive has an active disease (from `list_hives`' existing disease lookup); omitting `statuses` matches any of the four. Also the ticket where inactive-hive exclusion was added to the shared `resolveHives` helper, so `list_hives`/`list_hives_missing_records`/this tool all stopped surfacing inactive hives — single-hive lookups (`get_hive_summary`, `list_hive_records`) are unaffected since they authorize by hive_id directly, not through `resolveHives` (§3.3) |
| AST-06-BE | `BE`  | `[x]`  | Tool: `compare_hives`                    | `compare_hives(hive_ids)`: status flags, diseases, and each hive's latest inspection, treatment, feeding, and harvest — widened beyond the original inspection-only sketch since a "compare hive 3 and hive 7" question benefits from the full recent-activity picture, not just brood pattern. Authorized per hive_id — fails entirely if any hive_id isn't the caller's, same as get_hive_summary/list_hive_records (§3.3) |
| AST-07-BE | `BE`  | `[x]`  | Tool: `search_listings`                  | Thin wrapper over `ListingService.Search` — category/keyword/price_min/price_max/near_lat/near_lng/radius_km/has_apiary/limit/offset filters, no service-layer or REST changes. Results capped at 50 (default 20) to bound response size for the model (§3.3, §3.6) |
| AST-08-BE | `BE`  | `[x]`  | Tool: `get_listing`                      | Thin wrapper over `ListingService.Get(viewerUserID, listingID)` — hidden/pending listings stay visible only to their owner, same visibility rule as the REST endpoint (§3.3)                        |
| AST-16-BE | `BE`  | `[x]`  | Tool: `get_dashboard_summary`            | New capability, not a wrap of an existing endpoint — the app's "Dashboard" screen is a per-apiary PDF report generator, not a stats rollup. Composes existing per-hive repository calls into a cross-apiary aggregate: apiary count, active hive count, counts per status flag (queenless/needs_food/sick/ready_for_harvest), and recent inspection/treatment/feeding/harvest counts plus total kg harvested. Accepts optional `apiary_id` (scope to one apiary) and `days` (recency window; all-time if omitted), same day-window convention as AST-03/04/05-BE (§3.3) |
| AST-09-BE | `BE`  | `[x]`  | `POST /assistant/messages` — agent loop  | Claude Messages API + tool-use loop scoped to caller's `userID`, capped at 8 tool-call round trips; response streamed as SSE (`event: delta`/`done`/`error`) rather than a single JSON body — a mid-stream agent-loop failure is an `error` SSE event, not an HTTP error status, because headers are already committed to 200 by then. Route is conditionally registered in `main.go` — skipped (server still starts) if `ANTHROPIC_API_KEY` isn't set, via new `config.AnthropicAPIKey()`. Shipped stateless (client resends full history every call, no `conversation_id`); superseded by AST-19-BE once conversations persist (§3.2, §3.4). System prompt widened to also answer general beekeeping questions (e.g. "how do I treat varroa") from Claude's own knowledge, not just tool-backed questions about the caller's own data — previously the prompt implied everything had to come from a tool. Also fixed a turn-separator bug: consecutive agent-loop turns (filler text before a tool call, then the final answer) were concatenated with no separator, running them together; a `"\n\n"` join was added between turns |
| AST-10-BE | `BE`  | `[ ]`  | HTTP+SSE MCP transport at `/mcp`         | For future external MCP clients; in-app chat talks to tools directly, not over this transport (§3.2)            |
| AST-11-BE | `BE`  | `[ ]`  | Personal access token auth for external MCP clients | Deferred — no concrete external-client use case yet (§3.5)                                          |
| AST-12-FE | `FE`  | `[x]`  | `AssistantScreen` — chat UI              | Cubit pattern; message list + input field (§3.4)                                                                |
| AST-13-FE | `FE`  | `[x]`  | Nav drawer entry for Assistant           | New `AppSection`, signed-in only (§3.4)                                                                         |
| AST-14-FE | `FE`  | `[x]`  | Streaming response rendering             | Incremental display while the agent loop runs multiple tool calls (§3.4)                                        |
| AST-15-FE | `FE`  | `[x]`  | l10n strings for assistant chat UI       |                                                                                                                  |
| AST-17-BE | `BE`  | `[x]`  | Migration: `assistant_conversations` + `assistant_message_logs` + `assistant_tool_calls` | Widened from an audit-log-only sketch once we decided the app should support "just send the next message" instead of the client resending full history every call (see AST-19-BE) — these tables are now the source of truth for conversation reconstruction, not a side log. `assistant_conversations(id, user_id, created_at)` — one row per conversation. `assistant_message_logs(id, conversation_id, role, content, created_at)` — one row per user turn and per final assistant answer, ordered by `created_at` within a conversation. `assistant_tool_calls(id, message_id, tool_name, input jsonb, result jsonb, is_error, created_at)` — one row per MCP tool call the loop made while producing that assistant row. Model/repository landed this ticket (service in AST-18-BE); `input`/`result` use `gorm.io/datatypes.JSON` since the codebase had no prior JSONB convention; `internal/repository/assistant_test.go` uses `sqlmock` as the first repository-layer test, a pattern future repo tests should follow |
| AST-18-BE | `BE`  | `[x]`  | `AssistantService.Run` loads history and persists the trail | Given a `conversation_id`, loads its prior `assistant_message_logs` rows (authorization: the conversation must belong to the caller, same ownership-check pattern as everywhere else) instead of trusting a client-supplied history array; given none, creates a new `assistant_conversations` row. Inserts the new user message row before the loop starts, one tool-call row per `registry.Call` (name/input/result/is_error), and the final assistant message row once the loop ends. Logging failures must not fail the SSE response already in flight — log-and-continue, same as how a tool-call error becomes an `is_error` tool result rather than aborting the loop. Shipped together with AST-19-BE in one pass: this service-layer signature change forced the handler's wire contract to change too, so there was no way to land it alone |
| AST-19-BE | `BE`  | `[x]`  | Rework `POST /assistant/messages` request/response contract for conversation continuity | Request body becomes `{conversation_id?, message}` (a single new message) instead of `{messages: [...]}` (the full history) — the server now owns history via AST-18-BE. Response gains a new SSE event emitted before the first `delta`, e.g. `event: conversation` / `{"conversation_id": ...}`, so the client learns the id on a brand-new conversation and can pass it on the next call. An unknown or not-owned `conversation_id` is a 404 JSON error (`CONVERSATION_NOT_FOUND`) before any streaming starts, same as any other ownership check in this API. `EMPTY_MESSAGE` (400) replaces the old `EMPTY_MESSAGES`/`INVALID_LAST_MESSAGE` validation since there's no client-supplied array anymore. This is a breaking change to AST-09-BE's already-shipped contract — no FE consumer exists yet (AST-12-FE is still to-do), so there's no compatibility shim needed. Shipped together with AST-18-BE (see its note) |
| AST-20-BE | `BE`  | `[x]`  | Tool: `list_apiaries`                    | Fixes a gap found via manual testing: without a dedicated tool, the model could only infer apiaries by grouping hive results, so an apiary with zero hives was invisible. Returns id, name, caller's role, hive count, and last inspection date per apiary the caller belongs to, straight off the existing `ApiaryLister.ListByUserID` — no new repository code. Complements the same testing session's fix adding `apiary_name` to `HiveSummary` (previously hive/dashboard/comparison tools only exposed `apiary_id`, so the model resorted to fabricated labels like "Paseka 108") |
| AST-21-BE | `BE`  | `[x]`  | `GET`/`DELETE /assistant/conversations` endpoints, message cap | Chat history sidebar endpoints: `GET /assistant/conversations` (id/created_at/preview/message_count per conversation, newest first), `GET /assistant/conversations/{id}/messages` (full trail), and `DELETE /assistant/conversations/{id}` (cascades to its message logs/tool calls via FK) — all 404 `CONVERSATION_NOT_FOUND` if not owned/found, same ownership pattern as AST-18-BE/AST-19-BE. Also caps `POST /assistant/messages` at 10 logged messages per conversation (5 user + 5 assistant turns) to bound context size and API cost; once hit, further posts return 403 `CONVERSATION_LIMIT_REACHED` before any streaming starts |
| AST-22-FE | `FE`  | `[x]`  | Chat history as a persistent right-side panel | Went through a left-nav-drawer design first (indented sub-list under "Assistant", auto-open-on-navigate) but that fought Flutter's per-screen `Scaffold`/`Drawer` lifecycle — switching sections always destroys and recreates the drawer, so any attempt to keep/reopen it produced a visible close-then-reopen flicker. Landed instead on a plain always-mounted side panel local to `AssistantScreen` (`Row` of chat + `_AssistantHistoryPanel`, toggled via an AppBar `IconButton`, shown by default), sidestepping the Drawer animation entirely — no changes needed to `AuthenticatedAppDrawer`, `main.dart`, or any other section. Panel has a "New chat" row plus the conversation list (title = AST-21-BE's `preview`, trailing delete icon confirmed via the shared `showDeleteDialog` calling the new `DELETE` endpoint); refreshed from the widget layer (a `BlocConsumer` listener comparing `state.conversationId` across emissions) rather than from inside `AssistantCubit`, so the cubit's own unit tests stay exact-state-sequence assertable |

---

## 3. Shared Infrastructure

| ID     | Layer | Status | Title                          | Notes                                                                                          |
| ------ | ----- | ------ | ------------------------------- | ------------------------------------------------------------------------------------------------ |
| AI-01  | `BE`  | `[x]`  | New env vars                   | `internal/config/ai_config.go`'s `LoadAIConfig` covers `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` (Whisper is called via OpenAI's own API, so this is `OPENAI_API_KEY` rather than the originally-sketched `WHISPER_API_KEY`)/`AUDIO_STORAGE_PATH` via the existing `getEnv`-with-validation pattern. Not yet called from `main.go`'s startup config loading (§4). `ANTHROPIC_MODEL` added later (standalone `getEnv`, same style as `AnthropicAPIKey()`, not part of `LoadAIConfig`/`validate()`) — optional, defaults to `claude-haiku-4-5`; picks which Claude model `AssistantService`'s agent loop calls |
| AI-02  | `BE`  | `[ ]`  | `docs/api.md` updates          | All new endpoints across Epics 1 and 2, regenerate `docs/openapi.yaml` after                     |

---

## 4. Migrate to OpenRouter

> Both `AssistantService` (Epic 2, AST-09-BE) and the voice worker's Phase 1/2 calls (Epic 1,
> VC-08/09-BE) are written directly against the Anthropic Go SDK's native Messages API types
> (`anthropic.Message`, content blocks, `tool_use`/`tool_result`, SSE stream events) — not just
> a base-URL-and-key config. OpenRouter speaks an OpenAI-compatible chat-completions schema, so
> this is a schema migration through `internal/llm/`, both call sites, and their tests, not a
> config toggle. Goal/motivation (cost, rate limits, provider fallback) to be confirmed — scope
> below assumes "replace Anthropic direct API entirely," not "add as a fallback alongside it."
>
> Whisper transcription (`internal/llm/whisper.go`, VC-04-BE) moves too — user has both
> Anthropic and OpenAI keys set up as BYOK on OpenRouter. Confirmed via OpenRouter's own docs
> that `/api/v1/audio/transcriptions` is a real, OpenAI-compatible endpoint (multipart or
> base64, routes to `openai/whisper-1` and others) — this is a much lighter change than the
> chat migration since `whisper.go` already POSTs in the exact multipart shape that endpoint
> expects; mostly a base-URL/model-slug/key swap, not a schema rewrite (OR-08-BE). End state:
> a single `OPENROUTER_API_KEY` replaces both `ANTHROPIC_API_KEY` and `OPENAI_API_KEY`.

| ID       | Layer | Status | Title                                              | Notes                                                                                                     |
| -------- | ----- | ------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| OR-01-BE | `BE`  | `[x]`  | Swap `internal/llm/client.go` to an OpenRouter client | Landed additively as a new `internal/llm/openrouter_client.go` (`OpenRouterClient`) instead of touching `client.go` in place — hand-rolled HTTP client (no new go.mod dependency, matching `whisper.go`'s existing style) rather than the official Anthropic/OpenAI SDKs, since neither speaks OpenRouter's schema directly. `CreateChatCompletion` (non-streaming, for OR-04-BE) and `CreateChatCompletionStream` (SSE, for OR-03-BE, reassembles chunked `tool_calls` by index internally) both implemented with tests. Old Anthropic-based `client.go` untouched, still used by the voice worker until OR-04-BE |
| OR-02-BE | `BE`  | `[x]`  | Config: `OPENROUTER_API_KEY` / `OPENROUTER_MODEL` / `OPENROUTER_WHISPER_MODEL` | Added as new standalone getters in `internal/config/ai_config.go` (`OpenRouterAPIKey()`/`OpenRouterModel()` default `anthropic/claude-haiku-4.5`/`OpenRouterWhisperModel()` default `openai/whisper-1`), same `getEnv` pattern as the existing Anthropic getters — additive, `AnthropicAPIKey()`/`AnthropicModel()`/`OpenAIAPIKey()` left in place until OR-04/08-BE migrate their remaining callers (the voice worker) |
| OR-03-BE | `BE`  | `[x]`  | Migrate `AssistantService` agent loop to OpenAI-compatible schema | `internal/service/assistant.go`: replaced Anthropic content-block/`tool_use`/`tool_result` handling with flat OpenAI-style `messages`/`tool_calls` (`toolResultMessage` — no `is_error` flag on the wire, unlike Anthropic, errors are just conveyed as text content), `defaultAssistantModel` now an OpenRouter slug, system prompt moved from a separate `System` field into a `role: "system"` message. `streamTurnRunner` (in the same file, no separate `stream_turn_runner.go`) is now a thin forward to the new `chatStreamer` interface (`*llm.OpenRouterClient`) — all SSE chunk reassembly moved into and is tested by `internal/llm`'s `CreateChatCompletionStream` (OR-01-BE) instead of being re-implemented/re-tested here. `main.go`'s assistant-handler wiring switched from `AnthropicAPIKey()`/`llm.NewClient` to `OpenRouterAPIKey()`/`llm.NewOpenRouterClient`; `ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL`/`llm.NewClient`/`anthropic-sdk-go` no longer referenced anywhere in the assistant path — only the voice worker (OR-04-BE) still uses them |
| OR-04-BE | `BE`  | `[x]`  | Migrate voice worker's Phase 1/2 Claude calls      | `internal/worker/voice_worker.go`: `HiveNameResolver` (wrapped Anthropic's `Messages.New`) replaced with a schema-neutral `ChatCompleter` interface (`CreateChatCompletion`) satisfied directly by `*llm.OpenRouterClient`; all 5 `anthropic.ToolUnionParam`-returning tool builders collapsed through one new `functionTool(name, description, properties, required)` helper returning `llm.Tool`. Phase 1's forced single-tool call (previously `anthropic.ToolChoiceParamOfTool`) now uses `llm.ForceTool(name)`, a new `ToolChoice`/`ForceTool` addition to `internal/llm/openrouter_client.go` alongside `CreateChatCompletion` (OR-01-BE didn't need it yet, only the streaming path did). `logLLMCall`'s response param widened from `*anthropic.Message` to `any` since both phases now log a shared `*llm.ChatCompletionResponse`. `main.go`'s `startVoiceWorker` now requires `OPENROUTER_API_KEY` (chat) + `OPENAI_API_KEY` (Whisper, still direct until OR-08-BE) instead of `ANTHROPIC_API_KEY` + `OPENAI_API_KEY`. This was the last caller of the old Anthropic-based `internal/llm/client.go` — deleted it and its test outright (dead code once this landed) and ran `go mod tidy`, so `anthropic-sdk-go` is already fully gone from `go.mod`/`go.sum` ahead of the formal OR-07-BE check |
| OR-05-BE | `BE`  | `[x]`  | Update tests across `llm`/`service`/`worker`       | `voice_worker_test.go`'s `mockHiveResolver`/`resolveHiveMessage`/`toolUseMessage` and the tool-schema tests rewritten for the `llm` package types, completing what OR-01/03-BE started for `llm`/`service`/`handler`. `client_test.go` deleted along with `client.go` (OR-04-BE) rather than migrated, since the type it tested no longer exists |
| OR-06-BE | `BE`  | `[x]`  | Docs + env samples                                 | `.env.example` `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`ANTHROPIC_MODEL` lines removed (dead — nothing reads them since OR-04-BE), replaced by the existing `OPENROUTER_*` block; `docs/api.md` assistant-registration and voice-worker sections now name `OPENROUTER_API_KEY`/`OPENROUTER_MODEL`/`OPENROUTER_WHISPER_MODEL` instead of the old Anthropic/OpenAI vars; `AI_ASSISTANT.md` §4 table and §2.4 updated (OpenRouter client instead of "Anthropic API client", new env var row) plus a note on `voice_llm_calls.request`/`response` (VC-27-BE) switching from Anthropic Messages-API shape to OpenAI-compatible chat-completions shape as of OR-04-BE, with no version column to distinguish old rows |
| OR-08-BE | `BE`  | `[x]`  | Migrate `whisper.go` to OpenRouter's transcription endpoint | `internal/llm/whisper.go`: `defaultWhisperBaseURL` → `https://openrouter.ai/api/v1`; added a `model` field (new `WithWhisperModel` option, default `openai/whisper-1`) replacing the hardcoded `"whisper-1"` multipart field — `main.go`'s `startVoiceWorker` now passes `config.OpenRouterWhisperModel()`. `startVoiceWorker` also dropped its `OPENAI_API_KEY` requirement entirely: both `WhisperClient` and `OpenRouterClient` now authenticate with the same `OPENROUTER_API_KEY`, so the worker only warns/disables on that one key being unset. Auth header (`Authorization: Bearer <key>`) and multipart shape (`file`/`model`/`response_format=verbose_json`) unchanged, so `avg_logprob`/`no_speech_prob` parsing (VC-07-BE's quality gate) needed no changes — not yet confirmed against a live OpenRouter response, only against the existing httptest-based unit tests |
| OR-07-BE | `BE`  | `[x]`  | Verify Anthropic/OpenAI direct-API usage fully removed | `anthropic-sdk-go` was already gone from `go.mod`/`go.sum`/the codebase as of OR-04-BE. Confirmed no `api.openai.com`/`OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` references remain anywhere in `backend/`. Also removed the now-dead `AIConfig`/`LoadAIConfig`/`AnthropicAPIKey()`/`OpenAIAPIKey()`/`AnthropicModel()` from `internal/config/ai_config.go` (left in place by OR-02-BE for OR-04/08-BE's callers, which have since migrated off them — no remaining callers found) and trimmed `ai_config_test.go` to just the `OpenRouter*` getter tests. Final `go build ./...`, `go vet ./...`, and `go test ./...` all pass clean |
